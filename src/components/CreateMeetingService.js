
import React from "react";
import { useLocation } from "react-router-dom";
const CreateMeetingService=(props)=>
{
    const location = useLocation();
    const form = location.state;

    
    return
    (
        <>
        
        </>
    )
}

export default CreateMeetingService